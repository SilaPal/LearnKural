# Google OAuth credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs to add in Google Cloud Console:
#   http://localhost:3000/api/auth/google/callback    (local dev)
#   https://learnthirukkural.com/api/auth/google/callback  (production)

GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Callback URL — override if needed (defaults to the request origin)
# GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# In Replit: set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET as Replit Secrets
